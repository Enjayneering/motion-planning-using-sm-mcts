import numpy as np

confdict = {
    0: {'velocity': np.linspace(0, 1, 2).tolist(),
        'ang_velocity': np.linspace(-np.pi/2, np.pi/2, 3).tolist(),
        },
    1: {'velocity': np.linspace(0, 1, 2).tolist(),
        'ang_velocity': np.linspace(-np.pi/2, np.pi/2, 3).tolist(),
        },
    
}